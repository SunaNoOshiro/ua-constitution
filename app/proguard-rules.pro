# Project-specific ProGuard/R8 rules. Minification is disabled for release builds
# (isMinifyEnabled = false in app/build.gradle.kts), so no keep rules are required yet.
# Add rules here if R8 / minification is ever enabled.
